ansible_dependencies:
  - python3
  - unattended-upgrades
  - parted

update_command: apt update; unattended-upgrade
install_command: while [ -f /var/lib/dpkg/lock-frontend]; do sleep 2; done; apt install -y
